import styled from 'styled-components'
import tw from 'tailwind.macro'

/** Wrapper to center childs */
const PageCenterWrapper = styled.div`
  ${tw`flex flex-wrap justify-center`}
`

export { PageCenterWrapper }
